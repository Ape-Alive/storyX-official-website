{
  "openapi": "3.0.1",
  "info": {
    "title": "默认模块",
    "description": "",
    "version": "1.0.0"
  },
  "tags": [
    {
      "name": "认证"
    }
  ],
  "paths": {
    "/api/auth/one-time-token": {
      "post": {
        "summary": "生成一次性token（用于桌面端登录）",
        "deprecated": false,
        "description": "生成一次性token，用于桌面端登录。用户需要先通过正常登录获取accessToken，然后调用此接口生成一次性token。\n\n**使用场景：**\n- 用户已登录Web端，需要生成token供桌面端扫码登录\n- 管理员为用户生成一次性token\n\n**Token说明：**\n- 一次性token有效期：默认10分钟（可配置，最长60分钟）\n- 使用后自动失效，不能重复使用\n- 过期后自动失效\n\n**流程：**\n1. 用户通过Web端登录，获取accessToken\n2. 使用accessToken调用此接口，生成一次性token\n3. 将一次性token展示给用户（如二维码）\n4. 桌面端扫描二维码获取token，调用 /api/auth/desktop-login 登录\n",
        "tags": [
          "认证"
        ],
        "parameters": [],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": {
                  "expiresInMinutes": {
                    "type": "integer",
                    "minimum": 1,
                    "maximum": 60,
                    "default": 10,
                    "description": "过期时间（分钟），默认10分钟，最长60分钟",
                    "example": 10
                  }
                }
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "生成成功",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Success"
                },
                "example": {
                  "success": true,
                  "message": "One-time token generated successfully",
                  "data": {
                    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
                    "expiresAt": "2024-01-01T12:10:00.000Z",
                    "expiresIn": 600
                  }
                }
              }
            },
            "headers": {}
          },
          "401": {
            "description": "未认证或token无效",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Error"
                }
              }
            },
            "headers": {}
          }
        },
        "security": [
          {
            "bearerAuth": []
          }
        ]
      }
    }
  },
  "components": {
    "schemas": {
      "Error": {
        "type": "object",
        "properties": {
          "success": {
            "type": "boolean",
            "example": false
          },
          "message": {
            "type": "string",
            "example": "Error message"
          },
          "errors": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {}
            }
          }
        }
      },
      "Success": {
        "type": "object",
        "properties": {
          "success": {
            "type": "boolean",
            "example": true
          },
          "message": {
            "type": "string",
            "example": "Success message"
          },
          "data": {
            "type": "object",
            "properties": {}
          }
        }
      }
    },
    "responses": {},
    "securitySchemes": {
      "bearerAuth": {
        "type": "http",
        "scheme": "bearer",
        "bearerFormat": "JWT",
        "description": "输入 JWT Token，格式：Bearer {token}"
      }
    }
  },
  "servers": [],
  "security": []
}